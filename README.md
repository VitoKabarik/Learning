# Виджет для личного кабинета

Приложение, отображающее несколько последних успешных банковских операций клиента. Находится в разработке.

## Содержание:

- [Masks](masks)
- [Processing](processing)
- [Widjet](widjet)
- [Generators](generators)
- [Decorators](decorators)
- [Utils](utils)
- [Loader](loader)
- [External_API](external_api)
- [Тестирование](testing)
- [FAQ](faq)
- [To do](To_do)
- [Команда разработки](devs)

## <a id="masks">Masks</a>

Модуль содержит две функции: одна зашифровывает номер банковской карты клиента, а другая - номер его лицевого счёта. Отчёт об успешных и ошибочных случаях использования этой функции записывается в файл masks.log в папке logs, находящейся в корне проекта.

## <a id="processing">Processing</a>

Модуль содержит две функции: одна ищет в предложенном списке транзакции с определённым состоянием, а другая сортирует транзакции по дате.

## <a id="widjet">Widjet</a>

Модуль содержит две функции: одна определяет, имеет она дело с номером банковской карты или же номером лицевого счёта,
и зашифровывает его соответствующим образом, используя функции из модуля masks. Вторая функция принимает строку с датой и делает дату нагляднее.

## <a id="generators">Generators</a>

Модуль содержит три функции-генератора: первая по запросу выдаёт транзакции из предложенного списка, совершённые в указанной валюте, вторая по запросу показывает тип транзакций, а третья генерирует номера карт в указанном интервале.

## <a id="decorators">Decorators</a>

Модуль содержит декоратор log, который отслеживает состояние декорируемой функции. При нормальном завершении её работы декоратор сообщает время начала действия функции, её имя, а так же информирует о том, что она успешно завершила свою работу. Если же выполнение функции привело к ошибке, декоратор сообщает время начала действия, имя, тип возникшей ошибки, пояснение и входные параметры.
Отчёт записывается в файл, если декоратор получает его имя, или в консоль, если имени нет.

## <a id="utils">Utils</a>

Модуль содержит функцию, преобразующую json-файл по указанному пути в строку Python. Отчёт об успешных и ошибочных случаях использования этой функции записывается в файл utils.log в папке logs, находящейся в корне проекта.

## <a id="loader">Loader</a>

Модуль содержит две функции: одна считывает данные из csv-файла, а другая из excel-файла.

## <a id="external_api">External_API</a>

Модуль содержит функцию, которая получает на вход транзакцию и возвращает сумму этой транзакции в рублях.

## <a id="testing">Тестирование</a>

Функционал проекта покрыт Unit-тестами с использованием pytest. Для изоляции зависимостей и эмуляции файловой системы применялся unittest.mock. Проект имеет высокий уровень покрытия - 93%.

## <a id="faq">FAQ</a>

В проект добавлен файл .env.example, содержащий примеры токенов, необходимых для работы проекта.

## <a id="to_do">To do</a>

- [x] Начать разработку
- [x] Продолжить разработку
- [ ] Завершить разработку 

## <a id="devs">Команда разработки</a>

Виталий Москвин, начинающий бэкендер